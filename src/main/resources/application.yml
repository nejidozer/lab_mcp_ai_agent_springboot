spring:
  application:
    name: agent
server:
  port: 8080

github:
  owner: ${GITHUB_OWNER:demo_owner}
  repo: ${GITHUB_REPO:demo_repo}
  
anthropic:
  api-key: ${ANTHROPIC_API_KEY:demo_key}
  model: claude-opus-4-20250514
  timeout-seconds: 60
  
openai:
  api-key: demo
  model: gpt-4o-mini
  timeout-seconds: 60

mcp:
  base-url: http://localhost:3333
  path: /mcp
